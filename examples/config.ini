# Passwords are intentionally not allowed in this file.
# Export QB_MAIN_PASSWORD before running an audit.

[profile.main]
url = http://127.0.0.1:8080
username = admin
password_env = QB_MAIN_PASSWORD
timeout = 10
local = true
download_roots = /data/torrents, /srv/archive

